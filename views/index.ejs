<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Blog Home | J Tweet</title>
    <link rel="stylesheet" href="/styles.css" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
  </head>
  <body>
    <div class="blog">
      <%- include("partials/navbar.ejs") %>
      <main class="mt-4">
        <h1>Blog Posts</h1>
        <div class="posts rounded-sm bg-body-tertiary">
          <% if (posts.length === 0) { %>
            <p>No posts available. Create a new one!</p>
          <% } else { %> 
            <% posts.forEach((post) => { %>
              <div class="post shadow-sm bg-white rounded">
                <h2><%= post.title %></h2>
                <p><%= post.content %></p>

                <!-- edit button -->
                 <a href="/posts/<%= post.id %>/edit" class="btn btn-primary m-0 me-2">Edit</a>
                 <!-- delete button -->
                  <form action="/posts/<%= post.id %>?_method=DELETE" method="post" id="delete-form">
                    <button type="submit" class="btn btn-danger">Delete</button>
                  </form>
              </div>
            <% }); %> 
          <% } %>
        </div>
      </main>
      <%- include("partials/footer.ejs") %>
    </div>
    
  </body>
</html>
